# Stronger connection limit for admin/login
limit_conn addr 20;
limit_conn_status 429;

# Login-specific request rate limit
limit_req zone=logins burst=25 nodelay;
limit_req_status 429;

add_header Retry-After 5 always;